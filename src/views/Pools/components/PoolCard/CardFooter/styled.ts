import styled from 'styled-components'
import { Flex } from '@crosswise/uikit'

export const ExpandableButtonWrapper = styled(Flex)`
  align-items: center;
  justify-content: space-between;
  button {
    padding: 0;
  }
`
